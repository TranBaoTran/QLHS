package controller;

import at.favre.lib.crypto.bcrypt.*;
import model.user;

public class userDA {
    public int checkUser(user u){
        if(u != null){
            
        }
    }
    return -1;
}
